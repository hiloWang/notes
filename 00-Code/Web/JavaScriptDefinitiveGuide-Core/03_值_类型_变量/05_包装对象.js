//========================================================================
// 包装对象：理解包装对象
//========================================================================
var s = "test";//创建一个字符串
s.len = 5;//给s设置一个属性，Value assigned to primitive will be lost(分配给原语的值将丢失)
var t = s.len;
console.log(t);//打印结果为undefined
