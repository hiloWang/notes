<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.ztiany.test"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activity_animation.AnimActivity"/>
        <activity android:name=".activity_animation.AnimTwoActivity"/>
        <activity android:name=".tint.TintActivity"/>
        <activity android:name=".time.TimeActivity" android:theme="@style/Theme.AppCompat.Light"/>
        <activity android:name=".status.StatusBarActivity" android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <activity android:name=".fragments.FragmentsActivity"/>
        <activity android:name=".fragments.ViewPagerActivity"/>
        <activity android:name=".anr.ANRActivity"/>

    </application>

</manifest>